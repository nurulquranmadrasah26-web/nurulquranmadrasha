# সমস্যা সনাক্তকরণ ও সমাধান (আপডেট)

## ১) শিক্ষার্থী প্যানেল বারবার রিলোড হয়ে "কাঁপছিল" — মূল সমস্যা (ঠিক করা হয়েছে)
`admin_panel/auth.js` এর রোল-ভিত্তিক রিডাইরেক্ট পেজ সনাক্ত করত শুধু ফাইলনাম দিয়ে
(`student.html`)। কিন্তু ডিপ্লয়ে `vercel.json` পরিচ্ছন্ন ঠিকানা ব্যবহার করে
(`/student`, `/teacher`, `/`)। ফলে `/student` পেজে গিয়েও কোড ভাবত "এটা student.html নয়" →
আবার `/student` এ পাঠাত → অসীম রিডাইরেক্ট লুপ → স্ক্রিন কাঁপা/ব্ল্যাঙ্ক।

সমাধান:
- নতুন `currentPageKey()` — `/student`, `/student.html`, `/`, `/index.html`, `/admin` সবই সঠিকভাবে সনাক্ত করে।
- সঠিক পেজে থাকলে আর রিডাইরেক্ট হয় না।
- অতিরিক্ত নিরাপত্তা: `sessionStorage` কাউন্টার — কোনো কারণে দুইবারের বেশি রিডাইরেক্ট হলে থেমে যায় (লুপ-গার্ড)।

ফলাফল (ব্রাউজারে যাচাই করা): `/student` এ একবারেই লোড হয়, হেডার/নোটিশ/স্ট্যাট/বাড়ির পড়া সব রেন্ডার হয়।

## ২) লোগো ভাঙা আইকন দেখাচ্ছিল (ঠিক করা হয়েছে)
`assets/app-logo.png` ফাইলটি প্রজেক্টে নেই, তাই এডমিন/শিক্ষক/শিক্ষার্থী প্যানেল ও ওয়েবসাইটে
ভাঙা ছবি দেখাত। এখন `onerror` ফলব্যাক যোগ করা হয়েছে — ছবি না পাওয়া গেলে `icons/icon-192.png` দেখাবে।
(আসল লোগো `assets/app-logo.png` নামে রাখলে স্বয়ংক্রিয়ভাবে সেটিই দেখাবে।)

## ৩) `config.js` দুইবার লোড হচ্ছিল (ঠিক করা হয়েছে)
`student.html` ও `teacher.html` এ ডুপ্লিকেট `<script src="config.js">` সরানো হয়েছে — অপ্রয়োজনীয় রিকোয়েস্ট কমেছে।

## ৪) `renderHome()` এ ভেরিয়েবল শ্যাডোয়িং (ঠিক করা হয়েছে)
`today` ভেরিয়েবলটি একই ফাংশনে দুইবার ঘোষণা হচ্ছিল (একবার ISO, একবার বাংলা ফরম্যাট)।
এখন দ্বিতীয়টি `todayLabel` এবং আজকের পরিশোধ হিসাব দুই ফরম্যাটেই মেলে।

## যাচাই করা হয়েছে
- তিনটি প্যানেলের সব ইনলাইন স্ক্রিপ্ট + `auth.js` + `backend/server.js` — সিনট্যাক্স ঠিক আছে।
- `onclick`/`onchange` এ ব্যবহৃত সব ফাংশন সংজ্ঞায়িত আছে (কোনো missing handler নেই)।
- রোল রাউটিং টেস্ট: student → /student, teacher → /teacher, admin/superadmin → /  (প্রতিটিতে একবারেই, লুপ নেই)।

## পরামর্শ (কোড পরিবর্তন করা হয়নি)
- `backend/.env` ফাইলে গোপন তথ্য (MongoDB URI, JWT_SECRET, Cloudinary কী) আছে — এটি কখনো
  পাবলিক রিপোতে/জিপে শেয়ার করবেন না; Render এর Environment Variables এ রাখুন এবং কীগুলো রোটেট করে নিন।
